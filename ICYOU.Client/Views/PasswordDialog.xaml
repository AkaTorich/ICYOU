<Window x:Class="ICYOU.Client.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пароль базы данных" 
        Height="180" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="🔒 Локальная база зашифрована" 
                   FontSize="14" 
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" 
                   Text="Введите пароль для расшифровки:" 
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Margin="0,0,0,8"/>
        
        <PasswordBox x:Name="PasswordBox" 
                     Grid.Row="2"
                     Background="{DynamicResource SurfaceBrush}"
                     Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource SurfaceBrush}"
                     Padding="10,8"
                     VerticalAlignment="Top"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Пропустить" 
                    Click="Skip_Click"
                    Padding="15,8"
                    Margin="0,0,10,0"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource TextBrush}"
                    BorderThickness="0"
                    Cursor="Hand"/>
            <Button Content="OK" 
                    Click="OK_Click"
                    IsDefault="True"
                    Padding="20,8"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>

