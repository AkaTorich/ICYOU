<Window x:Class="ICYOU.Client.Views.NewChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новый групповой чат" 
        Height="550" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    
    <Border Background="{StaticResource SurfaceBrush}" 
            CornerRadius="16" 
            Margin="20"
            Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Создать групповой чат" 
                       Style="{StaticResource HeaderStyle}"
                       Margin="0,0,0,20"/>
            
            <!-- Название -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="Название чата" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="ChatName" Style="{StaticResource ModernTextBox}"/>
            </StackPanel>
            
            <!-- Заголовок списка друзей -->
            <TextBlock Grid.Row="2" 
                       Text="Выберите друзей для добавления:" 
                       Style="{StaticResource LabelStyle}"
                       Margin="0,0,0,10"/>
            
            <!-- Список друзей -->
            <Border Grid.Row="3" 
                    Background="{StaticResource CardBrush}" 
                    CornerRadius="8"
                    Padding="5">
                <ListBox x:Name="UsersList" 
                         Style="{StaticResource ChatListBox}"
                         ItemContainerStyle="{StaticResource ChatListBoxItem}"
                         SelectionMode="Multiple">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                          VerticalAlignment="Center"/>
                                
                                <Border Grid.Column="1" 
                                        Width="28" Height="28" 
                                        CornerRadius="14" 
                                        Background="{StaticResource PrimaryBrush}"
                                        Margin="8,0">
                                    <TextBlock Text="{Binding AvatarLetter}" 
                                               FontSize="12" 
                                               FontWeight="Bold"
                                               Foreground="White"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                </Border>
                                
                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}" 
                                               FontSize="14"
                                               Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="{Binding StatusText}" 
                                               FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            
            <!-- Кнопки -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Отмена" 
                        Style="{StaticResource SecondaryButton}"
                        Click="CancelButton_Click"
                        Margin="0,0,10,0"/>
                <Button Content="Создать" 
                        Style="{StaticResource PrimaryButton}"
                        Click="CreateButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

